# ============================================================
# API Lens — Environment Variables
# Copy this file to .env.local and fill every value
# NEVER commit .env.local to version control
# ============================================================

# SUPABASE — from supabase.com → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ENCRYPTION — generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be exactly 64 characters. Store this safely — if lost, all stored keys are unrecoverable.
ENCRYPTION_KEY=paste-64-character-hex-string-here

# RAZORPAY — from razorpay.com → Settings → API Keys
# Use test keys (rzp_test_...) while building, switch to live keys for launch
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-secret
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
# Plan IDs — create in Razorpay Dashboard → Subscriptions → Plans
# Monthly: ₹415/month | Annual: ₹4149/year
RAZORPAY_PLAN_MONTHLY=plan_xxxxxxxxxxxx
RAZORPAY_PLAN_ANNUAL=plan_xxxxxxxxxxxx

# RESEND — from resend.com → API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=hello@yourdomain.com
RESEND_FROM_NAME=API Lens

# APP
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=API Lens
# Generate cron secret: openssl rand -base64 32
CRON_SECRET=paste-random-string-minimum-32-characters

# OPTIONAL — add when ready
# NEXT_PUBLIC_POSTHOG_KEY=phc_your-key
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
